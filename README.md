# Neural-networks
This dataset contains multiple numerical features and one target variable used to train a neural network model. The data is cleaned, scaled, and split into training and testing sets to help the ANN learn patterns and make accurate predictions.
